
export const selectAccessToken = (state) => state.accounts.access_token;
export const selectVerifiedStatus = (state) => state.accounts.verified;
export const selectUserRole = (state) => state.accounts.role;
export const selectLoading = (state) => state.accounts.loading;
export const selectUserType = (state) => state.accounts.user_type;
